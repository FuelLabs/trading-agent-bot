services:
  trading-agent-bot:
    build: .
    container_name: trading-agent-bot
    restart: unless-stopped
    environment:
      - CONFIG_PATH=/app/config.yaml
    volumes:
      - ./config.yaml:/app/config.yaml:ro
